const NFTLabStore = artifacts.require("NFTLabStore");

contract("Testing NFTLabStore contract", async () => {
  it("Should mint the NFT", async () => {
    const instance = await NFTLabStore.deployed();
    
  });
});